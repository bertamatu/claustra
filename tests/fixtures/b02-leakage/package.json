{
  "name": "fixture-b02-leakage",
  "version": "0.0.0",
  "private": true,
  "//": "Fixture metadata only — never installed. claustra reads <scanned-project>/node_modules/next/package.json for version detection, not this manifest."
}
